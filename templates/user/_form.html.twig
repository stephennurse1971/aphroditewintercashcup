<style>
    .ck-content {
        min-height: 700px;
    }
</style>
{{ form_start(form) }}
<input type="hidden" name="referer" value="{{ app.request.headers.get('referer') }}">
<div class="row">
    <div class="col-12 col-lg-2">
        <h2 style="color: red">Personal Details</h2>
        {{ form_row(form.firstName) }}
        {{ form_row(form.lastName) }}
        {{ form_row(form.email) }}
        {{ form_row(form.mobile) }}
        {{ form_row(form.freeEntry) }}
        {{ form_row(form.emailVerified) }}
    </div>
    <div class="col-12 col-lg-2">
        <h2 style="color: red">Playing</h2>
        {{ form_row(form.playingSingles) }}
        {{ form_row(form.playingDoubles) }}
        {{ form_row(form.doublesPartner) }}
        {{ form_row(form.paidAmount) }}
        {{ form_row(form.subsidy) }}
        {{ form_row(form.paidTo) }}
        {{ form_row(form.paymentDate) }}
        {{ form_row(form.comments) }}
    </div>

    <div class="col-12 col-lg-2">
        <div class="password">
            <h2 style="color: red">Password</h2>
            {{ form_row(form.password) }}
        </div>
        {% if form.roles is defined %}
            <h2 style="color: red">Roles</h2>
            {{ form_row(form.roles) }}
        {% endif %}
    </div>


</div>

<button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

